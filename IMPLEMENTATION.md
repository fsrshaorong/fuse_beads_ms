# 实施范围

用户已确认纯前端快速迭代、精致微缩工作室、数字引导的点击/连续拖动放豆、最后熨烫，以及世界到桌面再到拼豆的连续镜头。联网不在此仓库范围。

2026-09-07 新增要求：指定柔光玩具房页面的渲染精确移植并关闭抖动，随后把网页版单独建仓并推送 `fsrshaorong/fuse_beads_ms`。本仓库依据这两项授权实施。

- 迁入旧 web-playground 样板、7 份纯值规则及其完整导入闭包、6 张纹理。构建和运行不读取父目录。
- 移植原站实际 GLSL、材质权重、灯光和输出设置。顶点不变形，纹理不随时钟跳动。现有工作室布局和拼豆规则继续使用。
- 通过 Application dispatch/read 验证玩法；通过同场景、同相机的原始着色代码对照并检查时间稳定性。
- 完成测试、生产构建、浏览器闭环和独立审查后，提交推送独立仓库。

## 回滚

渲染配置集中在 `src/Rendering/`，可用后续提交撤回对应更改。旧 Cocos 仓库仍保留此前样板基线；新仓库不迁移 `.meta`，不改 Cocos 正式场景。不要删除用户本地存档来回滚渲染。
